



import '../entry/stock_info_entry.dart';

Future<int> insertBook(StockInfoEntry gossipEntry) async {
  // if (gossipEntry.id != -1) {
  //   updateBook(book);
  //   return book.id;
  // }
  // final db = await DBHelper().database;
  // return db.insert('tb_books', book.toMap());

  return 0;
}